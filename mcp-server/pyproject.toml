[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backlog-client",
    "mcp[cli]==1.2.0rc1",
]

[tool.uv.sources]
backlog-client = { path = "../backlog-client/src" }
